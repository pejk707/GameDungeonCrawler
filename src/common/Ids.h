#pragma once

#include <string>

namespace ll {

// Идентификаторы — строки из файлов данных ("mine_pump", "oil_flask").
using RoomId = std::string;
using ItemId = std::string;
using EnemyId = std::string;
using ObjectId = std::string;
using FlagId = std::string;
using HintId = std::string;
using ZoneId = std::string;

}  // namespace ll
